import React from "react";
import "./Carousel.scss";

function Carousel() {
    return <div className="Carousel">
        {/* Here we need the heading and then the photos and then the scroll section */}
        
    </div>
}

export default Carousel;
